<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/layout">

    <fragment
        android:id="@+id/map"
        class="com.google.android.gms.maps.SupportMapFragment"
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />



    <TextView
        android:id="@+id/myLocation_notification_textView"
        android:paddingStart="10dp"
        android:paddingBottom="16dp"
        android:paddingTop="16dp"
        android:paddingEnd="65dp"
        android:fontFamily="monospace"
        android:textSize="14sp"
        android:textStyle="bold"
        android:layout_gravity="bottom|start"
        android:background="@android:color/white"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:maxHeight="180dp"
        android:scrollbars="vertical"
        android:text="Please wait till get your accurate location before you send..."/>


    <ImageView
        android:id="@+id/toggle_mapView"
        android:src="@drawable/ic_streetview_black_24dp"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_marginEnd="8dp"
        android:layout_gravity="bottom|end"
        android:layout_marginBottom="80dp"
        android:contentDescription="Toggle Map View" />

    <ImageView
        android:id="@+id/_sendLocation"
        android:src="@android:drawable/ic_menu_send"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_gravity="bottom|end"
        android:contentDescription="Toggle Map View" />
</FrameLayout>
